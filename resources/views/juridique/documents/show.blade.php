@extends('layouts.app')
@section('title', 'Détail document juridique')
@section('content')
<div class="content-wrapper"><div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h3 mb-0">{{ $document->titre }}</h1><div class="d-flex gap-2"><a href="{{ route('juridique.documents.edit', $document) }}" class="btn btn-warning">Modifier</a><a href="{{ route('juridique.documents.index') }}" class="btn btn-secondary">Retour</a></div></div><div class="card"><div class="card-body"><div class="row g-3"><div class="col-md-4"><strong>Référence</strong><div>{{ $document->reference }}</div></div><div class="col-md-4"><strong>Type</strong><div>{{ $document->type_document }}</div></div><div class="col-md-4"><strong>Statut</strong><div>{{ $document->statut }}</div></div><div class="col-md-6"><strong>Contrat lié</strong><div>{{ $document->contrat->titre ?? '-' }}</div></div><div class="col-md-3"><strong>Date document</strong><div>{{ optional($document->date_document)->format('d/m/Y') }}</div></div><div class="col-md-3"><strong>Date expiration</strong><div>{{ optional($document->date_expiration)->format('d/m/Y') }}</div></div><div class="col-12"><strong>Fichier</strong><div>@if($document->chemin_fichier)<a href="{{ asset('storage/' . $document->chemin_fichier) }}" target="_blank" class="btn btn-outline-primary btn-sm">Ouvrir le fichier</a><div class="text-muted small mt-2">{{ $document->chemin_fichier }}</div>@else<span class="text-muted">Aucun fichier</span>@endif</div></div><div class="col-12"><strong>Notes</strong><div>{{ $document->notes }}</div></div></div></div></div></div>
@endsection
